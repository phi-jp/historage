{
  "name": "historage",
  "version": "0.0.1",
  "description": "localStorage を使いやすくしたやつ",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.js --out-dir ./ --out-file historage.js --global Historage --no-cache --no-hmr",
    "build": "parcel build index.js --out-dir ./ --out-file historage.js --global Historage --no-cache --no-minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/phi-jp/historage.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/phi-jp/historage/issues"
  },
  "homepage": "https://github.com/phi-jp/historage#readme",
  "dependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
